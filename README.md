# Telegram-бот клуба (клиентский функционал)

Бот переносит клиентский функционал с фронта в Telegram: баланс, рулетка по коду клуба, история транзакций и призов, последние выигрыши.

## Стек

- Node.js (ES modules)
- [node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)
- axios для запросов к бэкенду

## Настройка

1. Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

2. В `.env`:

- `TELEGRAM_BOT_TOKEN` — токен от [@BotFather](https://t.me/BotFather) (у вас уже есть).
- `API_BASE_URL` — URL API бэкенда (по умолчанию прод: `https://club-back-production.up.railway.app/api`).

3. Установка и запуск:

```bash
cd telegram-bot
npm install
npm start
```

Для разработки с автоперезапуском:

```bash
npm run dev
```

## Команды бота

| Команда | Описание |
|--------|----------|
| `/start` | Приветствие и список команд |
| `/login` | Вход по номеру телефона и коду из СМС |
| `/balance`, `/баланс` | Показать баланс баллов (нужен вход) |
| `/spin <код>` | Крутить рулетку по коду клуба (например `/spin 123456`). Нужно 20 баллов и вход |
| `/prizes`, `/призы` | Мои выигранные призы (нужен вход) |
| `/history`, `/история` | История транзакций (нужен вход) |
| `/recent`, `/выигрыши` | Последние выигрыши по всем клубам (без входа) |
| `/logout` | Выйти |

## Авторизация

1. Пользователь отправляет `/login`.
2. Бот показывает кнопку **«📱 Отправить мой номер»** — номер подтягивается из Telegram автоматически.
3. Пользователь нажимает кнопку и подтверждает отправку контакта.
4. Бот сразу входит по номеру (без запроса кода) и сохраняет токен по Telegram user id в `store.json`. После перезапуска бота сессии сохраняются.

**Важно:** бэкенд должен принимать вход по номеру без кода (например, `POST /auth/login` с `{ phone }` и пустым или опциональным `code`). Если ваш API требует код, добавьте на бэкенде поддержку входа по телефону для Telegram.

## API бэкенда (как на фронте)

- `POST /auth/login` — вход (phone, code) → token
- `GET /players/balance` — баланс (Bearer)
- `GET /players/transactions` — транзакции (Bearer)
- `GET /players/prizes` — призы (Bearer)
- `GET /players/club?club=CODE` — клуб по коду/QR (публичный)
- `POST /players/spin` — спин рулетки (Bearer, 20 баллов)
- `GET /players/recent-wins` — последние выигрыши (публичный)

## Безопасность

- Токен бота хранится в `.env`; файл `.env` добавлен в `.gitignore` — не коммитьте его в репозиторий.
- Токены пользователей хранятся в `store.json` локально; для продакшена можно заменить на БД (Redis/Postgres).
